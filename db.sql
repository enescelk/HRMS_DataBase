-- This script was generated by a beta version of the ERD tool in pgAdmin 4.
-- Please log an issue at https://redmine.postgresql.org/projects/pgadmin4/issues/new if you find any bugs, including reproduction steps.
BEGIN;


CREATE TABLE public.users
(
    id integer,
    email character varying(40),
    password character varying,
    PRIMARY KEY (id)
);

CREATE TABLE public.job_seekers
(
    "userId" integer,
    "firstName" character varying,
    "lastName" character varying,
    "nationalId" bigint,
    "birthOfDate" date,
    PRIMARY KEY ("userId")
);

CREATE TABLE public.employers
(
    "userId" integer,
    "companyName" character varying,
    "webSites" character varying,
    "phoneNumber" character varying,
    PRIMARY KEY ("userId")
);

CREATE TABLE public."systemUsers"
(
    "userId" integer,
    "firstName" character varying,
    "lastName" character varying,
    PRIMARY KEY ("userId")
);

CREATE TABLE public."verificationStatus"
(
    id integer,
    "userId" integer,
    "emailVerifaction" boolean,
    "systemVerificatioon" boolean,
    PRIMARY KEY (id)
);

CREATE TABLE public."mernisVerifaction"
(
    id integer,
    "userId" integer,
    "mernisVerifaction" character varying,
    PRIMARY KEY (id)
);

CREATE TABLE public."jobPositions"
(
    id integer,
    "positionName" character varying,
    PRIMARY KEY (id)
);

ALTER TABLE public.users
    ADD FOREIGN KEY (id)
    REFERENCES public.job_seekers ("userId")
    NOT VALID;


ALTER TABLE public.users
    ADD FOREIGN KEY (id)
    REFERENCES public.employers ("userId")
    NOT VALID;


ALTER TABLE public.users
    ADD FOREIGN KEY (id)
    REFERENCES public."systemUsers" ("userId")
    NOT VALID;


ALTER TABLE public."verificationStatus"
    ADD FOREIGN KEY ("userId")
    REFERENCES public.employers ("userId")
    NOT VALID;


ALTER TABLE public."mernisVerifaction"
    ADD FOREIGN KEY ("userId")
    REFERENCES public.job_seekers ("userId")
    NOT VALID;

END;